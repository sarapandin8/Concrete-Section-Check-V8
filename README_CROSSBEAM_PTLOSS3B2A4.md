# CROSSBEAM.PTLOSS3B2A4 — Bond-state input, response-event audit, and mesh closeout

This milestone completes the remaining linear-response source hardening before compression-only contact work. It adds an explicit per-tendon Bond state to the canonical Tendon System and Project JSON schema without inferring it from Internal/External location, provides a controlled bulk assignment for active tendons, and keeps final AASHTO f_cgp/Elastic Shortening blocked until every active tendon has a compatible source. The fixed-base prestress case now publishes a station event audit that co-locates Section/Zone boundaries, column lines, tendon control-point roles, equivalent nodal forces/couples, local left/right moments, and the |N·Δy| reference-axis shift magnitude. Active-project mesh sensitivity at 0.50/0.25/0.125 m is automatically recomputed only when its isolated input fingerprint changes and is never persisted to Project JSON or downstream results. Stale milestone labels and duplicate scope text are removed. No frame, friction, anchorage-set, or Elastic Shortening equations are changed; compression-only contact, automatic lift-off, source-derived f_cgp, final ES, Pe/Pe_eff, Result Summary, and Report/QA credit remain locked.
