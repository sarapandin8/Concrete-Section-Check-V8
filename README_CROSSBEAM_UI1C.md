# CROSSBEAM.UI1C — Segment elevation readability polish

Scope:

- Move left/right anchorage labels outside the Crossbeam body and connect them to the anchorage markers with leader arrows.
- Add a compact engineering legend for Solid segment, Hollow segment, and Hidden void boundary.
- Keep visible segment labels compact (`Segment ID` + `Solid/Hollow`) and expose the full Section Builder preset, station range, and length in hover text.
- Preserve the full-length dashed hidden-void convention introduced in UI1B.
- Do not change segment assignments, Section Builder geometry, tendon data, solver paths, Project JSON, or existing non-Crossbeam workflows.
