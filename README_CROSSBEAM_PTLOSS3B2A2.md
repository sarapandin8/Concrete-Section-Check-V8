# CROSSBEAM.PTLOSS3B2A2 — Code-Basis Traceability and Print-Figure Integrity

This targeted milestone preserves the accepted PTLOSS3B2A1 linear-response equations and adds two review safeguards:

1. The Elastic Shortening workspace now displays the Portal Frame Crossbeam member-design code and the independent AASHTO LRFD 2020 prestress-loss basis as separate, explicit sources.
2. Every PTLOSS response chart is rendered through a print-targeted helper and CSS guard so the chart title and plot are moved together instead of being split across browser-print pages.

Continuous compression-only falsework contact, automatic lift-off, source-derived `f_cgp`, final Elastic Shortening, `Pe`/`Pe_eff`, Result Summary, and Report/QA handoff remain locked. No engineering equations or Project JSON schema were changed.
